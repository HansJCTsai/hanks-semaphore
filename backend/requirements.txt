# --- Web Framework Core ---
fastapi
uvicorn[standard]
pydantic-settings
python-multipart

# --- Database & ORM ---
sqlalchemy
alembic
asyncpg # 給 FastAPI (Async)
psycopg2-binary # [新增] 給 Celery Worker 和 Alembic (Sync)

# --- Task Queue & Broker [新增] ---
celery
redis
flower  # 推薦安裝，這是 Celery 的 Web 監控介面

# --- Security & Auth ---
passlib[bcrypt]
bcrypt==4.0.1
python-jose[cryptography]

# --- Utilities ---
requests
email-validator
httpx
PyYAML  # [新增] 用於讀取 config.yaml
black
typer # Typer and Rich are a powerful duo used to create modern, professional Command Line Interfaces (CLIs).
rich
shellingham # 有時候 typer 需要這個來檢測 shell
pyfiglet